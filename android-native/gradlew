#!/bin/sh

#
# Copyright © 2015-2021 the original author or authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

##############################################################################
#
#   IMPORTANT: DO NOT EDIT THIS FILE
#
#   This Gradle start up script for Unix was generated by Gradle.
#
#   Important for running:
#
#   (1) You need a Java JDK to run Gradle. The Java runtime (JRE) is not sufficient.
#       We recommend using the latest JDK version available.
#
#   (2) To use a specific JDK for Gradle, we recommend using the JAVA_HOME
#       environment variable.
#
#       Example:
#           export JAVA_HOME=/usr/local/jdk1.8.0_222
#
#   (3) For development we recommend to use the gradlew script to start Gradle.
#
#       Example:
#           ./gradlew tasks
#
#       This will use a local Gradle installation and add the project to the
#       Gradle cache.
#
#       If you do not have the gradlew script, you can generate it with:
#           gradle wrapper
#
#       See: https://docs.gradle.org/current/userguide/gradle_wrapper.html
#
#   (4) We recommend to use the gradlew script to start Gradle, but if you
#       have to use the gradle command directly, you can install a Gradle
#       distribution manually.
#
#       Example:
#           wget https://services.gradle.org/distributions/gradle-7.4-bin.zip
#           unzip gradle-7.4-bin.zip
#           export PATH=$PATH:$(pwd)/gradle-7.4/bin
#
#       See: https://gradle.org/install/
#
#   (5) For more information about the Gradle start up scripts, please refer
#       to the Gradle documentation.
#
#       See: https://docs.gradle.org/current/userguide/gradle_command_line.html
#
#
##############################################################################

# Attempt to set APP_HOME

# Resolve links: $0 may be a link
app_path=$0

# Need this for daisy-chained symlinks.
while
    APP_HOME=${app_path%"${app_path##*/}"}  # leaves a trailing / for empty result
    [ -h "$app_path" ] && [ "$APP_HOME" ]; do
    app_path=$APP_HOME
    cd "$app_path"
done

# This is normally unused
# shellcheck disable=SC2034
APP_BASE_NAME=${0##*/}
APP_HOME=$( cd "${APP_HOME:-./}" > /dev/null && pwd -P ) || exit

# Use the maximum available, or set MAX_FD != -1 to use that value.
MAX_FD=maximum

warn () {
    echo "$*"
} >&2

die () {
    echo
    echo "$*"
    echo
    exit 1
} >&2

# OS specific support (must be 'true' or 'false').
cygwin=false
msys=false
darwin=false
nonstop=false
case "$( uname )" in                #(
  CYGWIN* )         cygwin=true  ;;  #(
  Darwin* )         darwin=true  ;;  #(
  MSYS* | MINGW* )  msys=true     ;;  #(
  NonStop* )        nonstop=true  ;;
esac

CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar


# Determine the Java command to use to start the JVM.
if [ -n "$JAVA_HOME" ] ; then
    if [ -x "$JAVA_HOME/bin/java" ] ; then
        JAVACMD=$JAVA_HOME/bin/java
    else
        die "ERROR: JAVA_HOME is set to an invalid directory: $JAVA_HOME

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
    fi
else
    JAVACMD=java
    if ! command -v java >/dev/null 2>&1 ; then
        die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH.

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
    fi
fi

# Increase the maximum file descriptors if we can.
if ! "$cygwin" && ! "$darwin" && ! "$nonstop" ; then
    case $MAX_FD in #(
      max*)
        MAX_FD=$( ulimit -H -n ) ||
            warn "Could not query maximum file descriptor limit"
      ;;
    esac
    case $MAX_FD in  #(
      '' | soft) :;; #(
      *)              
        ulimit -n "$MAX_FD" ||
            warn "Could not set maximum file descriptor limit to $MAX_FD"
      ;;
    esac
fi

# Collect all arguments for the java command, following the shell quoting and substitution rules
eval set -- $DEBUG $JAVA_OPTS $GRADLE_OPTS \"\$@\" $JAVA_ARGS

# exec the java command
"$JAVACMD" "$@"
